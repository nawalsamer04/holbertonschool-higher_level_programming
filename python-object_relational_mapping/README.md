# Python - Object-relational mapping

This project connects Python to a MySQL database using:
- `MySQLdb` (mysqlclient) for raw SQL queries
- `SQLAlchemy` for ORM (later tasks)

The goal is to understand how SQL data maps to Python objects and how to query databases from Python.
